extends layout

block content
  h1= title
  //- const restaurant = ['Room_type','Duration','Cost']
  //- - let items = [] 
  - let items = results; 
  //- - let results;
  - let o;
  script.
    function buy(x){
      alert('Purchased Item: ' + x)
   } 
  div(class='container-fluid')
      each o, index in items 
        div(class='row')
          div(class='col-sm-3' onclick='buy('+index+')') #{o.names}
          div(class='col-sm-3' onclick='buy('+index+')') #{o.colour}
          div(class='col-sm-3' onclick='buy('+index+')') #{o.cost}